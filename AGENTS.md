# quick-image-agent-plugin 仓库协作规则

## 1. 仓库定位

- 本仓库是对外公开的开源仓库。必须假设所有被 Git 跟踪的内容、完整提交历史、Pull Request、Issue、构建产物和发布包都会被永久公开、复制和索引。
- 本仓库只包含宿主适配、共享 Skill、本地上传桥、公开 MCP 契约、安装文档和非敏感品牌资源。
- 同级仓库默认视为非公开实现边界。可以为了对齐公开契约而阅读，但不得复制或转述其内部代码、配置、拓扑、注释、日志或安全细节到本仓库。
- 不得把“不写入公开仓库”当作安全边界。必须假设攻击者完全知道 Plugin 源码、公开契约和客户端校验逻辑；权限、归属、计费、幂等、限流和最终输入校验必须由服务端强制执行。

## 2. 严禁公开的内容

以下内容不得出现在源码、注释、测试、Fixture、文档、示例、日志、截图、构建产物、提交信息、PR、Issue 或发布说明中：

- 任何真实凭据：API key、OAuth Token、refresh token、Cookie、Session、密码、client secret、签名密钥、私钥、证书、Webhook secret、云厂商凭据和供应商凭据。
- `.env`、配置值、账号 ID、用户数据、真实媒体、完整附件 URL、本地绝对路径、调试日志、崩溃报告和数据库导出。
- 非公开域名、IP、端口、内网拓扑、数据库结构、队列名、存储 bucket/key 规则、后台路径、供应商路由和内部服务名。
- 内部风控规则、反滥用阈值、限流阈值、告警阈值、封禁规则、人工复核方法和可被用于规避防护的判定逻辑。
- 未公开的漏洞细节、攻击链、可利用条件、真实攻击载荷、PoC、受影响内部端点和修复前绕过方法。
- 来自私有 Issue、安全事故、客服工单、内部会议、私有文档或其他仓库的敏感摘要。

不确定某项信息是否可公开时，按非公开内容处理，先停止写入或提交并向用户确认。

## 3. 公开契约与实现边界

- 只实现已批准的公开 MCP 工具、公开参数、稳定错误码和公开服务地址。不得为了方便调试而暴露内部字段、上游供应商响应、Blob ID 或服务端堆栈。
- 价格、权限、素材归属、幂等、扣费、任务状态和服务端媒体校验都属于服务端权威逻辑，不得复制到 Plugin 中形成可被绕过或推断的本地实现。
- 本地上传桥不得读取任意本地路径，不得接受任意上传 URL，不得跟随上传重定向，不得把文件 Base64、Token 或完整附件 URL 放入模型上下文或普通日志。
- 与安全有关的代码注释应描述必须保持的不变式，不描述内部漏洞的利用步骤或修复前缺口。

## 4. 测试、示例和日志

- 只使用人工构造的测试数据、随机 ID、`example.com` 域名、保留测试 IP 和虚假文件。禁止复制生产请求、用户附件、真实错误响应或脱敏不完全的日志作为 Fixture。
- 安全回归测试要验证“输入被拒绝”的行为，不提交可直接针对生产系统使用的完整漏洞 PoC。
- 日志和错误默认只记录稳定错误码、随机关联 ID 和必要的非敏感元数据。文件名、本地路径、URL query、headers、请求体和媒体元数据不得在未经审核的情况下记录。
- 禁止提交调试抓包、HAR、核心转储、崩溃文件、覆盖率原始数据、npm debug log 和包含本地绝对路径的 source map。

## 5. 提交与评审前必做检查

每次 commit 或发布前必须：

1. 检查 `git status --short`，确认没有误纳入 `.env`、日志、临时文件、媒体、抓包、编辑器状态或本地配置。
2. 同时检查未暂存和已暂存差异，不得只审查其中一份。
3. 对待提交内容执行 secret 扫描，发布前还必须扫描完整 Git 历史。扫描结果不得输出完整凭据，只报告文件、类型和脱敏片段。
4. 搜索生产域名、内网地址、本地绝对路径、私有仓库路径、内部类名和安全问题关键词，逐项确认它们是已批准的公开内容。
5. 检查新增二进制文件、压缩包、生成代码和 source map；无法人工审查的产物不得提交。
6. 发布 npm 包或 Plugin Bundle 前，先使用 dry-run/本地打包检查最终文件列表，再对解包内容做 secret 和内部标识扫描。

不得因为“稍后会删除”而先提交敏感内容。Git 历史中的删除不等于对外撤回。

## 6. Commit 信息规则

- Commit 必须使用 Conventional Commits，scope 相对于本仓库，描述使用中文。
- Commit subject 和 body 只描述公开可见的行为变化和保护目标，不记录漏洞的发现方式、利用条件、攻击载荷、内部端点、真实数据、防护阈值或事故细节。
- 安全修复使用中性、面向不变式的表述，例如 `fix(upload): 加强上传目标校验`，不要在提交信息中写明 SSRF 目标、内网地址、绕过方法或受影响生产系统。
- 不得在 commit message、branch 名、tag 名或公开 PR 标题中复制私有安全 Issue 标题或敏感事故描述。需要关联时，在非公开安全记录中单向关联公开 commit，不在公开 commit 中回链私有系统。
- 提交前必须单独审查最终 commit message，不得直接使用内部调试记录、AI 推理过程或工单内容作为 commit body。

## 7. 安全问题处理

- 发现疑似凭据、内部信息或未公开漏洞时，立即停止 commit、push、发布和公开 Issue/PR，只向用户报告风险类型、文件位置和已脱敏摘要。
- 如果代码 diff 本身会在防护生效前暴露可利用漏洞，不得先推送到公开分支。必须通过私有安全渠道协调服务端缓解、Plugin 修复、版本发布和公开披露顺序。
- 未获得明确授权前，不得通过重写 Git 历史、强制推送或删除远程引用来处理泄露。
- 如果真实凭据已进入提交或发布包，删除文件不足以完成处置；必须先通知用户轮换/撤销凭据，再按明确批准的方案处理历史。
- 未公开漏洞只通过私有安全渠道讨论。公开仓库中只提供负责任披露入口和已批准的公开说明。

## 8. `.gitignore` 与发布包基线

- 项目初始化时必须将 `.env`、`.env.*`、私钥、证书、日志、临时目录、覆盖率产物、编辑器本地状态和平台密钥文件加入 `.gitignore`。
- 不得使用否定规则将真实 `.env` 或凭据文件重新纳入 Git。需要示例时只提交不含真实值的 `.env.example`。
- npm package 使用 `package.json#files` 或同等允许列表限制发布内容；Plugin Bundle 同样应从显式允许列表构建，不得默认打包整个工作区。
